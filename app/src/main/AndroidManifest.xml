<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.musicplayer">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MusicPlayer">
        <activity android:name=".ventanaReproducir"></activity>
        <activity android:name=".detallesAlbumes" />
        <activity android:name=".ventanaAlbum" />
        <activity android:name=".DetallesArtistaActivity" />
        <activity android:name=".ventanaArtistas" />
        <activity android:name=".Configuracion" />
        <activity
            android:name=".activity_menu_principal"
            android:label="@string/title_activity_menu_principal" />
        <activity android:name=".ventanaCanciones" />
        <activity android:name=".ventanaRegistro" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>